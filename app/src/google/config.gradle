ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~9989845700", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/5649321612", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/8832676702",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/5833189839",        // 原生广告测试ID
                             full_native : "ca-app-pub-7869168547621070/5833189839",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/FlashNews_IV",
                       splash      : "/23312177113/FlashNews_SP",
                       native      : "/23312177113/FlashNews_NA",
                       full_native : "/23312177113/FlashNews_NA",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8856857", // Pangle测试应用ID
              adUnitIds    : [splash      : "890167296",        // 开屏广告测试ID（竖屏）
                              banner      : "",        // 横幅广告测试ID（320x50）
                              interstitial: "983368211",       // 插页广告测试ID（竖屏）
                              native      : "983368291",        // 原生广告测试ID
                              full_native : "983368291",        // 全屏原生广告测试ID
                              rewarded    : ""         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1hft92u12g87a",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "aa4a2285d6f6cd7b66cf43283f42f13f6",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1hft92uaprvg7",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "",  // 激励广告位 ID
                             native      : "n1hft92ugbp0t3",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1hft92uid7fv7",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1hft92ugbp0t3",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : ""]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.flashnews.liveheadlines.tool",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 1,
           versionName  : "1.0.0"]

    // 统计归因配置
    analytics = [adjustAppToken: "hopjs50gmuww",           // Adjust App Token
                 thinkingDataAppId: "871c0ebde6be4329b9b74acb3f08ba36",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "https://ds.devs343.com",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
